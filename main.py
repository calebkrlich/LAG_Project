import sys, getopt
from FileGenerator import FileGenerator

def main():
    #Read the command line args to specify where the files go
    #Pass the input file to the generator
    #Pass generate the output files
    g = FileGenerator("out")

main()